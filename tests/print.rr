load sys include;

function main() {
	Print("Ah yes, text " + string(69) + " also: " + string(gaming(true)));
	return;
}

function gaming(aaa bool) int {
    ok();

    if (aaa) {
        return (100 + 200);
    } else {
        return 200;
    }
}

function ok() {
    loop (10) {
        Print("ok");
    }
}
