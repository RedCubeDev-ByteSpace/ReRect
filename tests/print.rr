load sys include;

function main() {
	Print("Ah yes, text " + string(69) + " also: " + string(gaming(true)));
	return;
}

function gaming(aaa bool) int {
    if (aaa) {
        return (100 + 200);
    } else {
        return 200;
    }
}

//function gaming() gaming {
    // we gaming again
//}
